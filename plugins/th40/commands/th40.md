---
description: Control the TH40 keyboard lamps - status, bus slots, scan rate
---

Run the TH40 CLI with the user's arguments: `$ARGUMENTS`

```
!`"${CLAUDE_PLUGIN_ROOT}/bin/th40" $ARGUMENTS`
```

If no arguments were given, show the available commands and explain that slot 0
is Claude Code and slots 1-3 are free for anything else on the machine.
